import { Metadata } from "next";
import { FC } from "react";

const metadata: Metadata = {
  title: "Services | BD Nursing Care",
};

const ServicesPage: FC = () => <div>Service Page</div>;

export default ServicesPage;
export { metadata };
